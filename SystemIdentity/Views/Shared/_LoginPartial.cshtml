<ul class="navbar-nav">
    @if (User.Identity.IsAuthenticated)
    {
        <li class="nav-item dropdown">
            <div class="dropdown">
                <button class="btn btn-link dropdown-toggle" type="button" id="userOptionsDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Merhaba @User.Identity.Name,
                </button>
                <div class="dropdown-menu" aria-labelledby="userOptionsDropdown">
                    <h6 class="dropdown-header">Profil Seçenekleri</h6>
                    <a class="dropdown-item" asp-area="" asp-controller="Home" asp-action="UserPage">UserPage</a>
                    <a class="dropdown-item" asp-area="" asp-controller="Member" asp-action="PasswordChange">Şifreni Değiştir</a>
                @*   <a>
                        <li>
                            @if(User.HasClaim(x=>x.Value=="Admin")
                            {
                              <li><a class="dropdown-item" asp-controller="Member" asp-action="UserEdit"></a></li> 
                            };
                        </li>
                </a> //7Burda claim atarken indexx sayfasındaki rol sayfasına gidivcek burdan gittikten
                sorun kullanıcı yada yetkilendirme verilerini claime çevirecek
                *@
                    
                    <div class="dropdown-divider"></div>
                    <form asp-area="" asp-controller="Home" asp-action="Logout" method="post" onsubmit="changeButtonColor(this)">
                        <button type="submit" class="dropdown-item" id="logoutButton">Logout</button>
                    </form>
                </div>
            </div>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="btn btn-outline-success mx-1" asp-controller="Home" asp-action="Login">Oturum Aç</a>
        </li>
        <li class="nav-item">
            <a class="btn btn-outline-success" asp-controller="Home" asp-action="Register">Kayıt Ol</a>
        </li>
    }
</ul>
